# Optional. Without it the app uses OpenFreeMap's dark basemap, which needs no
# token and carries the real OSM building heights the 3D extrusions read.
# Setting this swaps the basemap for Mapbox dark-v11; no data layer changes.
NEXT_PUBLIC_MAPBOX_TOKEN=
